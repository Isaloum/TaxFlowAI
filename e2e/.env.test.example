# E2E test credentials — copy to .env.test (never commit .env.test)
# These accounts must exist in your database before running E2E tests

TEST_ACCOUNTANT_EMAIL=test-accountant@isaloumapps.com
TEST_ACCOUNTANT_PASSWORD=TestPass123!

TEST_CLIENT_EMAIL=test-client@isaloumapps.com
TEST_CLIENT_PASSWORD=TestPass456!

# Frontend base URL
BASE_URL=http://localhost:3000
