copilot/add-db-seed-workflow
name: Seed Database

name: Database Seed
main

on:
  workflow_dispatch:

jobs:
  seed:
    name: Seed Production Database
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
copilot/add-db-seed-workflow
      - name: Checkout
      - name: Checkout code
 main
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Generate Prisma Client
        working-directory: backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx prisma generate

 copilot/add-db-seed-workflow
      - name: Seed database

      - name: Run database seed
 main
        working-directory: backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npm run db:seed
 copilot/add-db-seed-workflow
 - name: Verify seed success
        run: echo "✅ Database seeded successfully with test user ihab@taxflowai.com"
main
